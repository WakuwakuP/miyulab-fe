webxr-polyfill.js:34048 [Immersive Web Emulator] native WebXR API successfully overridden
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected local|https://pl.waku.dev
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected local|https://prismisskey.space
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected public|https://prismisskey.space
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://prismisskey.space|ステラソラ
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://prismisskey.space|原神
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://prismisskey.space|genshin
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://prismisskey.space|gochisou_photo
0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11 connected userStreaming
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected public|https://pl.waku.dev
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://pl.waku.dev|ステラソラ
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://pl.waku.dev|原神
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://pl.waku.dev|genshin
0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442 connected tag|https://pl.waku.dev|gochisou_photo
0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11 connected userStreaming
102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297 SQLite Worker: using OPFS SAH Pool persistence
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = SELECT p.id FROM posts p
         JOIN profiles pr ON pr.id = p.author_profile_id
         JOIN servers s ON s.id = pr.server_id
         JOIN posts orig ON orig.id = p.reblog_of_post_id
         WHERE p.is_reblog = 1
           AND orig.object_uri = ?
           AND pr.username = ?
           AND (s.host = ? OR pr.actor_uri LIKE ?)
         LIMIT 1;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
p @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:120
（匿名） @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:157
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 Failed to initialize for https://pl.waku.dev: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = SELECT p.id FROM posts p
         JOIN profiles pr ON pr.id = p.author_profile_id
         JOIN servers s ON s.id = pr.server_id
         JOIN posts orig ON orig.id = p.reblog_of_post_id
         WHERE p.is_reblog = 1
           AND orig.object_uri = ?
           AND pr.username = ?
           AND (s.host = ? OR pr.actor_uri LIKE ?)
         LIMIT 1;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
p @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:120
（匿名） @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:157
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 Failed to flush upsert buffer: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
i @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in i
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
setTimeout
r @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
（匿名） @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442
a.emit @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:3
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
l.emit @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:3
onMessage @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
e._callEventListener @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
_handleMessage @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 779 SQLITE_CORRUPT_INDEX SQL = UPDATE posts SET
        last_fetched_at        = ?,
        visibility_id          = ?,
        language               = ?,
        content_html           = ?,
        spoiler_text           = ?,
        canonical_url          = ?,
        is_reblog              = ?,
        is_sensitive           = ?,
        in_reply_to_uri        = ?,
        in_reply_to_account_acct = ?,
        edited_at_ms           = ?,
        plain_content          = ?,
        quote_state            = ?,
        is_local_only          = ?,
        application_name       = ?,
        reblog_of_post_id      = ?,
        quote_of_post_id       = ?
      WHERE id = ?;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
p @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:128
（匿名） @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:157
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 Failed to fetch initial data for tag (https://prismisskey.space): Error: SQLITE_CORRUPT_INDEX: sqlite3 result code 779: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442
Promise.catch
（匿名） @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442
e @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442
Promise.finally
e @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = SELECT p.id FROM posts p
         JOIN profiles pr ON pr.id = p.author_profile_id
         JOIN servers s ON s.id = pr.server_id
         JOIN posts orig ON orig.id = p.reblog_of_post_id
         WHERE p.is_reblog = 1
           AND orig.object_uri = ?
           AND pr.username = ?
           AND (s.host = ? OR pr.actor_uri LIKE ?)
         LIMIT 1;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
p @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:120
（匿名） @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:157
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 Failed to flush upsert buffer: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
i @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in i
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
setTimeout
r @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
（匿名） @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442
a.emit @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:3
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
l.emit @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:3
onMessage @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
e._callEventListener @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
_handleMessage @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1 sqlite3_step() rc= 11 SQLITE_CORRUPT SQL = 
  SELECT 
  p.id AS post_id,
  COALESCE((SELECT la2.backend_url FROM local_accounts la2 WHERE la2.id = spb.local_account_id), '') AS backendUrl,
  spb.local_id AS local_id,
  p.created_at_ms,
  p.object_uri,
  COALESCE(p.content_html, '') AS content_html,
  COALESCE(p.spoiler_text, '') AS spoiler_text,
  p.canonical_url,
  p.language,
  COALESCE(vt.name, 'public') AS visibility_code,
  p.is_sensitive,
  (p.reblog_of_post_id IS NOT NULL) AS is_reblog,
  p.in_reply_to_uri AS in_reply_to_id,
  p.edited_at_ms,
  COALESCE(pr.acct, '') AS author_acct,
  COALESCE(pr.username, '') AS author_username,
  COALESCE(pr.display_name, '') AS author_display_name,
  COALESCE(pr.avatar_url, '') AS author_avatar,
  COALESCE(pr.header_url, '') AS author_header,
  COALESCE(pr.is_locked, 0) AS author_locked,
  COALESCE(pr.is_bot, 0) AS author_bot,
  COALESCE(pr.url, '') AS author_url,
  COALESCE(ps.replies_count, 0) AS replies_count,
  COALESCE(ps.reblogs_count, 0) AS reblogs_count,
  COALESCE(ps.favourites_count, 0) AS favourites_count,
  rs.id AS rb_post_id,
  COALESCE(rs.content_html, '') AS rb_content_html,
  COALESCE(rs.spoiler_text, '') AS rb_spoiler_text,
  rs.canonical_url AS rb_canonical_url,
  rs.language AS rb_language,
  COALESCE(rvt.name, 'public') AS rb_visibility_code,
  rs.is_sensitive AS rb_is_sensitive,
  rs.in_reply_to_uri AS rb_in_reply_to_id,
  rs.edited_at_ms AS rb_edited_at,
  rs.created_at_ms AS rb_created_at_ms,
  rs.object_uri AS rb_object_uri,
  COALESCE(rpr.acct, '') AS rb_author_acct,
  COALESCE(rpr.username, '') AS rb_author_username,
  COALESCE(rpr.display_name, '') AS rb_author_display_name,
  COALESCE(rpr.avatar_url, '') AS rb_author_avatar,
  COALESCE(rpr.header_url, '') AS rb_author_header,
  COALESCE(rpr.is_locked, 0) AS rb_author_locked,
  COALESCE(rpr.is_bot, 0) AS rb_author_bot,
  COALESCE(rpr.url, '') AS rb_author_url,
  COALESCE(rps.replies_count, 0) AS rb_replies_count,
  COALESCE(rps.reblogs_count, 0) AS rb_reblogs_count,
  COALESCE(rps.favourites_count, 0) AS rb_favourites_count,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id AND rpb.server_id = spb.server_id LIMIT 1),
      (SELECT rpb.local_id FROM post_backend_ids rpb WHERE rpb.post_id = rs.id ORDER BY rpb.server_id LIMIT 1)
    )
    ELSE NULL
  END AS rb_local_id,
  COALESCE(la_auth.remote_account_id, '') AS author_account_id,
  CASE WHEN rs.id IS NOT NULL
    THEN COALESCE(
      (SELECT la3.remote_account_id FROM post_backend_ids rpb3 INNER JOIN local_accounts la3 ON la3.id = rpb3.local_account_id WHERE rpb3.post_id = rs.id AND rpb3.server_id = spb.server_id LIMIT 1),
      '')
    ELSE ''
  END AS rb_author_account_id,
  ps.emoji_reactions_json,
  rps.emoji_reactions_json AS rb_emoji_reactions_json
  FROM posts p
  
  LEFT JOIN profiles pr ON p.author_profile_id = pr.id
  LEFT JOIN visibility_types vt ON p.visibility_id = vt.id
  LEFT JOIN post_backend_ids pb ON p.id = pb.post_id
  LEFT JOIN post_stats ps ON p.id = ps.post_id
  LEFT JOIN posts rs ON p.reblog_of_post_id IS NOT NULL AND rs.id = p.reblog_of_post_id
  LEFT JOIN profiles rpr ON rs.author_profile_id = rpr.id
  LEFT JOIN visibility_types rvt ON rs.visibility_id = rvt.id
  LEFT JOIN post_stats rps ON rs.id = rps.post_id
  LEFT JOIN post_backend_ids spb
    ON spb.post_id = p.id
    AND spb.server_id = (
      SELECT MIN(spb_min.server_id)
      FROM post_backend_ids spb_min
      WHERE spb_min.post_id = p.id
        AND spb_min.server_id IN (SELECT la.server_id FROM local_accounts la WHERE la.backend_url IN ('https://pl.waku.dev','https://prismisskey.space'))
    )
  LEFT JOIN local_accounts la_auth ON la_auth.id = spb.local_account_id
  WHERE p.id IN (?,?,?,?,?,?,?)
  GROUP BY p.id
  ORDER BY p.created_at_ms DESC;
step @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
exec @ 087dtuwaz31cw.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:1
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:259
（匿名） @ 0drsbpij0eho..js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
self.onmessage @ 102ytw4a220ar.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:297
installHook.js:1 [useGraphTimeline] fetch error: Error: SQLITE_CORRUPT: sqlite3 result code 11: database disk image is malformed
    at Worker.E (0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:24973)
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
e @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
d @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
setTimeout
h @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:8
E @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266
installHook.js:1 Failed to perform initial periodic cleanup Error: Worker request timed out (id=213, type=enforceMaxLength)
    at 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:26362
overrideMethod @ installHook.js:1
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in （匿名）
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
installHook.js:1 Failed to flush upsert buffer: Error: Worker request timed out (id=214, type=bulkUpsertStatuses)
    at 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:266:26362
overrideMethod @ installHook.js:1
i @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
await in i
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
setTimeout
r @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
（匿名） @ 0zuv4dfx.3w_j.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:442
a.emit @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:3
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
l.emit @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:3
onMessage @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
e._callEventListener @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
（匿名） @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
_handleMessage @ 0y4br5ie~g83u.js?dpl=dpl_FgYVMMKVUvZrmJQaZ9UnjrboVsfX:11
